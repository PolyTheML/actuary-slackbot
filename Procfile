---
web: gunicorn app:app --bind 0.0.0.0:$PORT --timeout 120
---

INSTRUCTIONS:
1. Create a new file called "Procfile" (no extension, capital P)
2. Paste ONLY the line above (the "web: gunicorn..." line)
3. Save it

The file should contain exactly one line with no extra spaces or line breaks at the end.